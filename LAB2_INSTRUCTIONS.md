# 📋 Инструкции для выполнения лабораторной работы 2

## ✅ Что уже сделано автоматически

1. ✅ Создан отдельный сервис `users_service` с собственной БД
2. ✅ Обновлен `backend` - убрана модель User, удалены FK на users
3. ✅ Созданы миграции для обоих сервисов
4. ✅ Настроен Nginx как API Gateway
5. ✅ Обновлен `docker-compose.yml` для микросервисной архитектуры
6. ✅ Обновлена аутентификация - JWT токен теперь включает `user_id`

## 🔧 Что нужно сделать вам

### 1. Проверка SECRET_KEY

**Важно**: Оба сервиса должны использовать **одинаковый SECRET_KEY** для валидации JWT токенов.

Проверьте файлы:
- `src/config.py` (backend)
- `users_service/src/config.py` (users-api)

Убедитесь, что `SECRET_KEY` одинаковый в обоих файлах, или используйте переменную окружения:

```env
SECRET_KEY=your-secret-key-change-in-production
```

### 2. Миграция данных (если есть существующие данные)

Если у вас уже есть данные пользователей в старой БД, нужно их мигрировать:

#### Вариант A: Через SQL dump

```bash
# Экспорт данных из старой БД
docker exec -i msa-db-1 pg_dump -U user -d blog_platform -t users > users_dump.sql

# Импорт в новую БД users
docker exec -i <db-users-container> psql -U app -d app_users < users_dump.sql
```

#### Вариант B: Через Python скрипт

Создайте скрипт `migrate_users.py`:

```python
# migrate_users.py
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
import sys

# Старая БД
old_db = create_engine("postgresql://user:password@localhost:5432/blog_platform")
old_session = sessionmaker(bind=old_db)()

# Новая БД
new_db = create_engine("postgresql://app:app@localhost:5432/app_users")
new_session = sessionmaker(bind=new_db)()

# Копирование данных
old_users = old_session.execute("SELECT * FROM users").fetchall()
for user in old_users:
    new_session.execute(
        "INSERT INTO users (id, email, username, password_hash, bio, image_url, is_active, created_at, updated_at) "
        "VALUES (:id, :email, :username, :password_hash, :bio, :image_url, :is_active, :created_at, :updated_at)",
        dict(user)
    )

new_session.commit()
print("Migration completed!")
```

### 3. Запуск и тестирование

#### Шаг 1: Запуск всех сервисов

```bash
docker-compose up --build
```

Это запустит:
- `db-main` - база данных для backend (articles, comments)
- `db-users` - база данных для users-api
- `backend` - сервис статей и комментариев
- `users-api` - сервис пользователей
- `api-gateway` - Nginx на порту 80

#### Шаг 2: Проверка работы сервисов

```bash
# Проверка health checks
curl http://localhost/health

# Проверка users-api
curl http://localhost/api/users

# Проверка backend
curl http://localhost/api/articles
```

#### Шаг 3: Тестирование полного цикла

1. **Регистрация пользователя**:
```bash
curl -X POST http://localhost/api/users \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "username": "testuser",
    "password": "password123"
  }'
```

2. **Вход и получение токена**:
```bash
curl -X POST http://localhost/api/users/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123"
  }'
```

Сохраните `access_token` из ответа.

3. **Создание статьи** (используйте токен из шага 2):
```bash
curl -X POST http://localhost/api/articles \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -d '{
    "title": "Test Article",
    "description": "Test Description",
    "body": "Test Body Content"
  }'
```

4. **Получение статей**:
```bash
curl http://localhost/api/articles
```

### 4. Проверка архитектуры

#### Проверка, что users вынесены в отдельный сервис:

```bash
# Должен работать через API Gateway
curl http://localhost/api/users

# Должен работать напрямую (внутренний порт)
curl http://localhost:8000/api/users  # НЕ должен работать (порт не проброшен)
```

#### Проверка, что FK на users удалены:

```bash
# Подключитесь к БД backend
docker exec -it <backend-db-container> psql -U app -d app_main

# Проверьте структуру таблиц
\d articles
\d comments

# Должны увидеть, что author_id НЕ имеет FK constraint
```

### 5. Проверка маршрутизации Nginx

Проверьте логи API Gateway:

```bash
docker-compose logs api-gateway
```

Убедитесь, что запросы правильно маршрутизируются:
- `/api/users/*` → `users-api:8000`
- `/api/user/*` → `users-api:8000`
- Все остальное → `backend:8000`

## 🐛 Troubleshooting

### Проблема: JWT токен не валидируется

**Причина**: Разные SECRET_KEY в сервисах

**Решение**: Убедитесь, что `SECRET_KEY` одинаковый в:
- `src/config.py` (backend)
- `users_service/src/config.py` (users-api)

Или используйте переменную окружения в `docker-compose.yml`:

```yaml
environment:
  - SECRET_KEY=${SECRET_KEY}
```

### Проблема: База данных не подключается

**Решение**: 
1. Проверьте, что контейнеры БД запущены:
```bash
docker-compose ps
```

2. Проверьте логи:
```bash
docker-compose logs db-main
docker-compose logs db-users
```

### Проблема: Миграции не выполняются

**Решение**: 
1. Проверьте логи:
```bash
docker-compose logs backend
docker-compose logs users-api
```

2. Выполните миграции вручную:
```bash
# Backend
docker-compose exec backend alembic upgrade head

# Users API
docker-compose exec users-api alembic upgrade head
```

### Проблема: API Gateway не маршрутизирует запросы

**Решение**:
1. Проверьте конфигурацию Nginx:
```bash
docker-compose exec api-gateway cat /etc/nginx/nginx.conf
```

2. Проверьте, что сервисы доступны:
```bash
docker-compose exec api-gateway curl http://backend:8000/health
docker-compose exec api-gateway curl http://users-api:8000/health
```

## 📝 Контрольные вопросы

1. **Почему у каждого микросервиса должна быть своя база данных?**
   - Изоляция данных и независимость сервисов
   - Возможность масштабирования каждого сервиса отдельно
   - Разные технологии БД для разных сервисов

2. **Почему внешние ключи между сервисами — антипаттерн?**
   - Нарушает принцип data ownership
   - Создает жесткую связь между сервисами
   - Усложняет развертывание и масштабирование

3. **Как можно обеспечить согласованность данных между сервисами без FK?**
   - Через API вызовы
   - Event-driven архитектура
   - Saga pattern для распределенных транзакций

4. **Что такое data ownership и зачем оно нужно?**
   - Каждый сервис владеет своими данными
   - Обеспечивает независимость и масштабируемость
   - Упрощает поддержку и развитие

5. **Что произойдёт, если `users-api` недоступен при запросе постов?**
   - Backend может работать независимо
   - JWT токены можно валидировать без обращения к users-api
   - Но создание новых пользователей будет недоступно

## ✅ Acceptance Criteria Checklist

- [x] users вынесен в отдельный микросервис с собственной БД
- [x] Связи user_id хранятся без FK
- [x] Gateway корректно маршрутизирует `/users/*`
- [x] 2 БД, 2 приложения, 1 API Gateway
- [x] Код читаемый, структура логичная
- [x] docker-compose работает
- [ ] (Опционально) Реализована миграция данных
- [ ] (Опционально) Healthchecks работают
- [ ] (Опционально) Graceful shutdown

## 🎯 Итог

После выполнения всех шагов у вас будет полноценная микросервисная архитектура с:
- Изолированными базами данных
- API Gateway для маршрутизации
- Независимыми сервисами
- Правильным data ownership pattern

